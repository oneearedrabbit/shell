{
  "license": "MIT",
  "scripts": {
    "lint": "prettier --check . && eslint --ignore-path .gitignore . && flake8",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "svelte": "^3.29.0",
    "eslint": "^7.22.0",
    "eslint-config-prettier": "^8.1.0",
    "eslint-plugin-svelte3": "^3.1.0",
    "prettier-plugin-svelte": "^2.2.0",
    "prettier": "^2.3.1"
  }
}
